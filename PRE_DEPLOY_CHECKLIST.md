# ✅ Чеклист перед деплоем

## 🔍 Проверка проекта

### 1. Безопасность
- [x] `.env` файлы добавлены в `.gitignore`
- [x] `node_modules/` добавлены в `.gitignore`
- [x] Созданы `.env.example` файлы
- [x] Нет приватных ключей в коде
- [x] `deployments.json` можно коммитить (содержит только публичные адреса)

### 2. Конфигурация
- [x] `package.json` настроен правильно
- [x] `frontend/package.json` настроен правильно
- [x] `hardhat.config.js` настроен
- [x] `vercel.json` создан
- [x] `.gitignore` обновлен
- [x] `.gitattributes` создан

### 3. Контракты
- [x] Контракты скомпилированы
- [x] Контракты задеплоены в Sepolia
- [x] Адреса контрактов сохранены в `deployments.json`
- [x] Адреса контрактов:
  - JobMarketplace: `0x57f315E773788e087f9123ccE6515BeD9BD7520F`
  - SalaryNegotiation: `0x08faCE30dc5538344ae275d128D2b92fFb8a492E`

### 4. Фронтенд
- [x] Зависимости установлены
- [ ] Фронтенд собирается без ошибок (нужно проверить)
- [x] Переменные окружения настроены через `.env.example`
- [x] Хук `useFhevm.js` обновлен (убраны зависимости от FHEVM)

### 5. Документация
- [x] `README.md` обновлен
- [x] `DEPLOYMENT.md` создан
- [x] Инструкции по деплою готовы

## 📝 Следующие шаги

### Для GitHub:
1. Инициализируйте git репозиторий (если еще не сделано)
2. Добавьте все файлы
3. Сделайте первый коммит
4. Добавьте remote (когда получите URL от пользователя)
5. Запушьте на GitHub

### Для Vercel:
1. Установите переменные окружения в Vercel:
   - `REACT_APP_JOBMARKETPLACE_ADDRESS`
   - `REACT_APP_SALARYNEGOTIATION_ADDRESS`
2. Подключите GitHub репозиторий к Vercel
3. Настройте build settings:
   - Root Directory: `frontend`
   - Build Command: `npm run build`
   - Output Directory: `build`
4. Деплой!

## ⚠️ Важно помнить

- **НИКОГДА** не коммитьте `.env` файлы
- Проверяйте `git status` перед каждым коммитом
- Используйте `.env.example` как шаблон
- Адреса контрактов можно коммитить (они публичные)

