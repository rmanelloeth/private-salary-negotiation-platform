# Инструкции по деплою

## 🚀 Деплой на Vercel

### Вариант 1: Через Vercel Dashboard

1. **Подготовьте проект:**
   ```bash
   cd frontend
   npm run build
   ```

2. **Загрузите на Vercel:**
   - Зайдите на [vercel.com](https://vercel.com)
   - Нажмите "New Project"
   - Импортируйте ваш GitHub репозиторий
   - Настройте проект:
     - **Root Directory:** `frontend`
     - **Build Command:** `npm run build`
     - **Output Directory:** `build`
     - **Install Command:** `npm install`

3. **Настройте переменные окружения:**
   В настройках проекта Vercel добавьте:
   - `REACT_APP_JOBMARKETPLACE_ADDRESS` = `0x57f315E773788e087f9123ccE6515BeD9BD7520F`
   - `REACT_APP_SALARYNEGOTIATION_ADDRESS` = `0x08faCE30dc5538344ae275d128D2b92fFb8a492E`

4. **Деплой:**
   - Vercel автоматически задеплоит при каждом пуше в main ветку
   - Или нажмите "Deploy" вручную

### Вариант 2: Через Vercel CLI

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите в аккаунт
vercel login

# Задеплойте
cd frontend
vercel

# Для продакшена
vercel --prod
```

## 📦 Подготовка к GitHub

### 1. Проверьте .gitignore

Убедитесь, что следующие файлы НЕ попадут в репозиторий:
- `.env` (с приватными ключами)
- `node_modules/`
- `artifacts/`
- `cache/`
- `deployments.json` (опционально, можно закоммитить)

### 2. Создайте .env.example файлы

Уже созданы:
- `.env.example` в корне
- `frontend/.env.example`

### 3. Инициализируйте Git репозиторий

```bash
# Инициализация
git init

# Добавьте все файлы
git add .

# Первый коммит
git commit -m "Initial commit: Private Salary Negotiation Platform"

# Добавьте remote (когда получите URL от пользователя)
# git remote add origin https://github.com/username/repo.git

# Запушьте
# git push -u origin main
```

### 4. Что НЕ коммитить

❌ **НИКОГДА не коммитьте:**
- `.env` файлы с приватными ключами
- `node_modules/`
- Личные данные
- Приватные ключи в любом виде

✅ **Можно коммитить:**
- `deployments.json` (содержит только публичные адреса контрактов)
- `.env.example` файлы
- Весь исходный код
- Конфигурационные файлы

## 🔐 Безопасность

1. **Проверьте перед пушем:**
   ```bash
   git status
   git diff
   ```

2. **Убедитесь что .env не в индексе:**
   ```bash
   git check-ignore .env
   # Должно вывести: .env
   ```

3. **Если случайно добавили .env:**
   ```bash
   git rm --cached .env
   git commit -m "Remove .env from tracking"
   ```

## 📝 Чеклист перед пушем

- [ ] `.env` файлы добавлены в `.gitignore`
- [ ] `node_modules/` добавлены в `.gitignore`
- [ ] Созданы `.env.example` файлы
- [ ] `README.md` обновлен с актуальной информацией
- [ ] Контракты задеплоены и адреса обновлены
- [ ] Фронтенд собирается без ошибок (`npm run build`)
- [ ] Нет приватных ключей в коде
- [ ] `vercel.json` настроен правильно

## 🌐 После деплоя на Vercel

1. Проверьте что сайт работает
2. Убедитесь что переменные окружения установлены
3. Проверьте подключение к кошельку
4. Протестируйте взаимодействие с контрактами

## 🔄 Обновление после изменений

1. Внесите изменения в код
2. Протестируйте локально
3. Закоммитьте и запушьте:
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push
   ```
4. Vercel автоматически задеплоит новую версию

