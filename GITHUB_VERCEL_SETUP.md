# 🚀 Инструкции по настройке GitHub и Vercel

## 📦 Подготовка к GitHub

### Шаг 1: Инициализация Git репозитория

```bash
# Инициализируйте git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Проверьте что .env НЕ добавлен
git status
# Убедитесь что .env не в списке!

# Если .env случайно добавлен, удалите его из индекса:
# git rm --cached .env

# Сделайте первый коммит
git commit -m "Initial commit: Zama Salary Negotiation Platform"
```

### Шаг 2: Создание репозитория на GitHub

1. Зайдите на [github.com](https://github.com)
2. Нажмите "New repository"
3. Заполните:
   - **Repository name:** `zama-salary-negotiation-platform` (или любое другое имя)
   - **Description:** "Private Salary Negotiation Platform using Zama FHEVM"
   - **Visibility:** Public или Private (на ваш выбор)
   - **НЕ** добавляйте README, .gitignore или license (они уже есть)
4. Нажмите "Create repository"

### Шаг 3: Подключение и пуш

```bash
# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/zama-salary-negotiation-platform.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Запушьте код
git push -u origin main
```

## 🌐 Деплой на Vercel

### Вариант 1: Через Vercel Dashboard (Рекомендуется)

1. **Зайдите на [vercel.com](https://vercel.com)**
   - Войдите через GitHub аккаунт

2. **Импортируйте проект:**
   - Нажмите "Add New..." → "Project"
   - Выберите ваш GitHub репозиторий
   - Нажмите "Import"

3. **Настройте проект:**
   - **Framework Preset:** Create React App
   - **Root Directory:** `frontend` (важно!)
   - **Build Command:** `npm run build` (или оставьте по умолчанию)
   - **Output Directory:** `build` (или оставьте по умолчанию)
   - **Install Command:** `npm install` (или оставьте по умолчанию)

4. **Настройте переменные окружения:**
   В разделе "Environment Variables" добавьте:
   ```
   REACT_APP_JOBMARKETPLACE_ADDRESS = 0x57f315E773788e087f9123ccE6515BeD9BD7520F
   REACT_APP_SALARYNEGOTIATION_ADDRESS = 0x08faCE30dc5538344ae275d128D2b92fFb8a492E
   ```
   - Выберите все окружения (Production, Preview, Development)

5. **Деплой:**
   - Нажмите "Deploy"
   - Дождитесь завершения сборки
   - Получите URL вашего приложения

### Вариант 2: Через Vercel CLI

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите в аккаунт
vercel login

# Задеплойте (из корня проекта)
vercel

# Следуйте инструкциям:
# - Set up and deploy? Y
# - Which scope? (выберите ваш аккаунт)
# - Link to existing project? N
# - Project name? (оставьте по умолчанию или введите свое)
# - Directory? frontend
# - Override settings? N

# После первого деплоя, настройте переменные окружения:
vercel env add REACT_APP_JOBMARKETPLACE_ADDRESS
# Введите: 0x57f315E773788e087f9123ccE6515BeD9BD7520F

vercel env add REACT_APP_SALARYNEGOTIATION_ADDRESS
# Введите: 0x08faCE30dc5538344ae275d128D2b92fFb8a492E

# Задеплойте в продакшен
vercel --prod
```

## 🔄 Автоматический деплой

После подключения GitHub к Vercel:
- Каждый пуш в `main` ветку автоматически задеплоит в Production
- Pull requests автоматически создадут Preview деплои
- Все деплои будут видны в Vercel Dashboard

## ✅ Проверка после деплоя

1. **Откройте ваш сайт на Vercel**
2. **Проверьте консоль браузера** (F12) на ошибки
3. **Попробуйте подключить кошелек** (MetaMask)
4. **Проверьте что адреса контрактов загружаются правильно**

## 🔧 Настройка кастомного домена (опционально)

1. В Vercel Dashboard → Settings → Domains
2. Добавьте ваш домен
3. Следуйте инструкциям по настройке DNS

## 📝 Полезные команды

```bash
# Проверить статус git
git status

# Посмотреть что будет закоммичено
git diff --cached

# Проверить что .env не в репозитории
git check-ignore .env
# Должно вывести: .env

# Обновить Vercel деплой вручную
vercel --prod
```

## ⚠️ Важные напоминания

- ✅ **МОЖНО коммитить:** `deployments.json`, весь код, конфигурационные файлы
- ❌ **НЕЛЬЗЯ коммитить:** `.env` файлы, приватные ключи, `node_modules/`
- 🔐 Всегда проверяйте `git status` перед коммитом
- 🌐 Переменные окружения настраиваются в Vercel Dashboard, не в коде

## 🆘 Решение проблем

### Проблема: Vercel не может собрать проект
- Проверьте что Root Directory установлен в `frontend`
- Убедитесь что переменные окружения установлены
- Проверьте логи сборки в Vercel Dashboard

### Проблема: Контракты не загружаются
- Проверьте что переменные окружения установлены в Vercel
- Убедитесь что адреса контрактов правильные
- Проверьте консоль браузера на ошибки

### Проблема: Кошелек не подключается
- Убедитесь что MetaMask установлен
- Проверьте что вы на правильной сети (Sepolia)
- Проверьте консоль браузера на ошибки

## 📞 Готово к деплою!

Когда будете готовы, дайте мне знать ваш GitHub username, и я помогу с финальными шагами!

