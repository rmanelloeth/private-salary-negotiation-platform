{
  "name": "zama-salary-negotiation-dapp",
  "version": "1.0.0",
  "description": "Private Salary Negotiation & Job Matching Platform using Zama FHEVM",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network fhenix",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "node": "hardhat node",
    "test": "hardhat test",
    "frontend": "cd frontend && npm start",
    "build": "cd frontend && npm run build"
  },
  "keywords": ["zama", "fhevm", "blockchain", "privacy", "salary"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "ethers": "^6.8.0"
  }
}

